```tsx


const RecipeTable: React.FC = () => {
  const columns: GridColDef[] = [
    { field: 'id', headerName: 'ID', width: 90 },
    { field: 'name', headerName: 'Recipe Name', width: 200 },
    { field: 'ingredients', headerName: 'Ingredients', width: 300 },
    { field: 'section', headerName: 'Section', width: 150 }, // Add section column
  ];

  const rows: GridRowsProp = [
    { id: 1, name: 'Recipe 1', ingredients: 'ingredient1, ingredient2', section: 'Appetizer' },
    { id: 2, name: 'Recipe 2', ingredients: 'ingredient1, ingredient3', section: 'Main Course' },
    // More rows...
  ];

  return (
    <div style={{ height: 400, width: '100%' }}>
      <DataGrid
        rows={rows}
        columns={columns}
        pageSize={5}
        pageSizeOptions={[5, 10, 25, 50, 100]}
      />
    </div>
  );
};

const CreateRecipe: React.FC = () => {
  const [recipeName, setRecipeName] = useState('');
  const [section, setSection] = useState(''); // Manage section state

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    const recipeData = {
      recipe_name: recipeName,
      section: section, // Include section in the form data
    };

    // Send POST request to save recipe with section
    const response = await fetch('/save-recipes', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(recipeData),
    });

    if (response.ok) {
      alert('Recipe saved successfully!');
    } else {
      alert('Failed to save recipe.');
    }
  };

  return (
    <div>
      <h2>Create Recipe</h2>
      <form onSubmit={handleSubmit}>
        <label>Section:</label>
        <input
          type="text"
          value={section}
          onChange={(e) => setSection(e.target.value)}
          required
        />

        <label>Recipe Name:</label>
        <input
          type="text"
          value={recipeName}
          onChange={(e) => setRecipeName(e.target.value)}
          required
        />

        <button type="submit">Save Recipe</button>
      </form>
    </div>
  );
};

def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('recipe', schema=None) as batch_op:
        batch_op.add_column(sa.Column('section', sa.String(length=255), nullable=True))  # Changed to nullable=True
        batch_op.alter_column('recipe_total_cost',
               existing_type=sa.NUMERIC(),
               type_=sa.Float(),
               nullable=False,
               existing_server_default=sa.text('0'))

    # ### end Alembic commands ###

